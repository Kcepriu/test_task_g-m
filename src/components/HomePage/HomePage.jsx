import { ListCards } from "../ListCards/ListCards";

import { Container } from "./HomePage.styled";

export const HomePage = () => {
  return (
    <Container>
      <ListCards />
    </Container>
  );
};
